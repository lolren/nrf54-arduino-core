This directory is reserved for a staged connectedhomeip intake.

Populate it with:

  scripts/import_connectedhomeip_scaffold.sh <connectedhomeip-ref>

The presence of this directory does not mean a compileable Matter target
exists. It only freezes the intended staging path in-tree.
